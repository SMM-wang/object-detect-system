# 航拍图像智能检测系统 - 项目说明书（V1.0）

## 一、项目概述

### 1.1 项目背景

随着无人机航拍、遥感巡检、智慧交通、城市治理和基础设施巡检等场景的发展，航拍图像和视频数据规模持续增长。传统人工判读方式存在效率低、主观性强、漏检率高、难以批量处理等问题，无法满足高频巡检和快速响应的业务需求。

本项目拟建设一套“航拍图像智能检测系统”，通过前端可视化交互、后端 AI 推理服务、数据库持久化管理和智能研判能力，实现对航拍图片及视频中的目标进行自动检测、结果展示、历史记录管理和辅助分析。

### 1.2 项目目标

系统建设目标如下：

1. 支持用户登录、权限校验和基础用户状态管理。
2. 支持航拍图片、视频及标准数据集目录的上传与检测。
3. 支持检测模型、置信度阈值、IoU 阈值等参数配置。
4. 基于 YOLO 系列目标检测模型完成航拍目标智能识别。
5. 使用 Canvas 在前端精准绘制检测框、置信度标签和目标轨迹。
6. 支持 AI 研判面板，对检测结果生成场景分析说明。
7. 支持检测记录查询、分页、筛选和结果回溯。
8. 支持检测统计分析，为后续系统评估和业务决策提供依据。
9. 提供明确的接口、数据库、测试和验收标准，便于开发、测试和交付。

### 1.3 适用场景

系统适用于以下典型场景：

- 无人机航拍目标检测与结果标注。
- 遥感图像中的车辆、建筑物、道路设施等目标识别。
- 电力、交通、园区、工地等场景的巡检图像分析。
- 标准航拍数据集的模型测试与结果评估。
- 目标检测算法演示、教学、实验和工程化验证。

### 1.4 预期成果

项目完成后应交付一套可运行的软件系统，包含：

- Vue 3 前端可视化系统。
- Flask 后端接口服务。
- YOLO/ONNX 推理模块。
- MySQL 数据库表结构及初始化脚本。
- RESTful API 接口说明。
- 测试用例、测试报告和验收说明。
- 项目部署运行文档。

## 二、总体需求

### 2.1 系统定位

航拍图像智能检测系统由前端展示层、后端服务层、AI 推理层、数据持久层和智能研判层组成。

- 前端展示层负责用户操作、参数配置、文件上传、检测结果渲染和统计展示。
- 后端服务层负责接口转发、鉴权校验、文件解析、任务调度和异常处理。
- AI 推理层负责加载目标检测模型并输出检测结果。
- 数据持久层负责保存用户信息、检测日志、模型配置和结果快照。
- 智能研判层负责基于检测结果生成辅助分析文本。

### 2.2 用户角色

| 角色 | 说明 | 主要权限 |
| --- | --- | --- |
| 普通用户 | 使用系统完成图像或视频检测 | 登录、上传文件、配置参数、查看本人检测记录 |
| 管理员 | 管理系统数据和模型配置 | 用户管理、记录管理、模型配置、统计查看 |
| 系统维护人员 | 部署和维护系统运行环境 | 后端服务维护、模型文件维护、数据库维护 |

### 2.3 业务流程

系统核心业务流程如下：

1. 用户访问系统登录页面，输入账号和密码。
2. 后端完成账号校验并返回鉴权 Token。
3. 前端保存 Token，并通过路由守卫控制业务页面访问权限。
4. 用户进入工作台，上传航拍图片、视频或测试数据集。
5. 用户配置检测模型、置信度阈值和 IoU 阈值。
6. 前端调用检测接口，将文件和参数提交至后端。
7. 后端解析文件，调用 AI 推理模块完成目标检测。
8. 后端返回检测框坐标、类别、置信度、耗时和 AI 研判文本。
9. 前端使用 Canvas 绘制检测结果，并展示 AI 研判内容。
10. 后端将检测日志、结果快照和耗时信息写入数据库。
11. 用户可在检测记录页面按时间、类别、结果等条件查询历史数据。
12. 系统可基于历史记录生成检测数量、类别分布、耗时趋势等统计图表。

### 2.4 系统边界

本系统主要处理航拍图像与视频的目标检测、结果可视化和记录管理，不承担无人机飞控、实时航线规划、硬件采集控制等功能。外部大模型服务仅作为辅助研判能力接入，系统核心检测功能不依赖大模型接口的成功返回。

## 三、功能需求

### 3.1 登录鉴权模块

#### 3.1.1 功能说明

系统应提供用户登录能力，对核心业务页面进行访问控制，防止未授权用户访问检测、记录和统计页面。

#### 3.1.2 功能要求

1. 登录页面支持账号和密码输入。
2. 后端对密码进行 bcrypt 摘要校验。
3. 登录成功后返回 Token、用户 ID、用户名和角色信息。
4. 前端将 Token 存储在 LocalStorage 或 SessionStorage 中。
5. Vue Router 路由守卫应在访问 `/dashboard`、`/detect`、`/records`、`/statistics` 等页面前校验 Token。
6. Axios 请求拦截器应自动在 Header 中携带 Token。
7. 响应拦截器应统一处理 401、403、500 等异常状态。
8. Token 失效时应提示用户重新登录并跳转至登录页。

### 3.2 工作台与文件上传模块

#### 3.2.1 功能说明

工作台用于承载核心检测操作，用户可上传航拍图片、视频或标准数据集目录，并查看上传状态和检测结果。

#### 3.2.2 功能要求

1. 前端使用 Element Plus 的 `el-upload` 组件实现文件上传。
2. 支持拖拽上传图片文件，格式包括 `.jpg`、`.jpeg`、`.png`。
3. 支持上传视频文件，格式包括 `.mp4`。
4. 支持批量导入兼容 VisDrone2019、AI-TOD-v2 等标准数据集格式的测试目录。
5. 上传前应校验文件类型、文件大小和文件数量。
6. 上传过程中应展示加载状态和进度反馈。
7. 上传失败时应展示明确错误提示。
8. 文件名在后端必须经过 `werkzeug` 安全过滤。
9. 图片文件应优先使用内存缓冲方式处理，避免产生大量临时磁盘碎片。

### 3.3 检测参数配置模块

#### 3.3.1 功能说明

用户可在检测前配置模型版本和推理阈值，以适配不同检测任务和性能需求。

#### 3.3.2 功能要求

1. 支持模型版本选择，包括基线模型和轻量化模型。
2. 支持配置置信度阈值，取值范围为 0.01 至 1.0。
3. 支持配置 IoU 阈值，取值范围为 0.01 至 1.0。
4. 参数配置应采用联动表单组件实现。
5. 前端应将参数随检测请求一起提交给后端。
6. 后端应对参数范围进行校验，非法参数返回 400 错误。
7. 当前选中的模型和参数应纳入全局状态管理。

### 3.4 AI 目标检测模块

#### 3.4.1 功能说明

后端应基于 YOLO 系列目标检测模型，对上传图像或视频帧进行目标检测，并返回标准化检测结果。

#### 3.4.2 功能要求

1. 后端技术栈采用 Python 3.10+、Flask、OpenCV、ONNX Runtime GPU 或 TensorRT。
2. Flask 主进程启动时应完成 `YOLODetector` 实例化和模型预热。
3. 模型权重文件应在服务启动阶段加载至 GPU 显存。
4. 禁止在单次 API 请求中重复加载模型。
5. 图片检测接口应返回目标类别、坐标、置信度和推理耗时。
6. 视频检测应支持按帧抽取、逐帧检测和结果汇总。
7. 后端应对图像解码失败、模型推理失败和显存不足进行异常捕获。
8. 检测结果应以 JSON 格式返回，便于前端 Canvas 渲染和数据库存储。

### 3.5 检测结果可视化模块

#### 3.5.1 功能说明

前端应在原始图像或视频画面上展示检测结果，包括检测框、类别名称、置信度和目标轨迹。

#### 3.5.2 功能要求

1. 前端必须使用 HTML5 Canvas API 绘制检测结果。
2. 禁止使用大量 DOM 元素叠加检测框。
3. Canvas 绘制时应基于原图尺寸和显示尺寸进行坐标换算。
4. 检测框应展示类别名称和置信度。
5. 多目标检测结果应使用不同颜色或清晰样式区分。
6. 视频场景下应支持目标轨迹或逐帧检测结果展示。
7. 图片缩放或窗口尺寸变化时，检测框应保持与图像位置一致。
8. 检测结果为空时应明确提示“未检测到目标”。

### 3.6 AI 研判模块

#### 3.6.1 功能说明

系统应基于检测结果生成辅助分析文本，用于提升系统智能化展示效果和结果解释能力。

#### 3.6.2 功能要求

1. 后端应将检测结果中的坐标、类别和置信度拼接为结构化 Prompt。
2. 后端通过独立异步函数或后台线程调用外部 LLM API。
3. 大模型调用不得阻塞核心推理流程。
4. 当前端接收到 AI 分析文本后，应在右侧 AI 研判面板展示。
5. 前端应支持打字机效果，实现逐字输出。
6. 大模型接口失败时，不应影响检测结果展示。
7. 大模型异常应记录日志，并向前端返回可理解的提示信息。

### 3.7 检测记录管理模块

#### 3.7.1 功能说明

系统应保存每次检测的基础信息、检测结果和耗时信息，并提供查询、筛选和分页展示能力。

#### 3.7.2 功能要求

1. 前端使用 Element Plus 的 `el-table` 展示检测记录。
2. 支持按检测时间范围查询。
3. 支持按目标类别或缺陷类型筛选。
4. 支持按用户、模型版本、检测状态查询。
5. 必须使用后端真实服务端分页，禁止仅在前端做假分页。
6. 支持查看检测详情，包括原图、检测结果、AI 研判和耗时。
7. 支持管理员查看全部记录，普通用户仅查看本人记录。
8. 查询条件变化时应重新请求后端接口。

### 3.8 统计分析模块

#### 3.8.1 功能说明

系统应基于历史检测记录提供基础统计分析能力，帮助用户了解检测任务和目标分布情况。

#### 3.8.2 功能要求

1. 前端使用 ECharts 展示统计图表。
2. 支持检测任务总数统计。
3. 支持不同类别目标数量统计。
4. 支持检测耗时趋势统计。
5. 支持按时间范围筛选统计结果。
6. 支持展示模型版本使用占比。
7. 统计数据应由后端聚合返回，避免前端拉取大量原始记录后计算。

### 3.9 全局状态与网络请求模块

#### 3.9.1 功能说明

前端应统一管理用户状态、检测参数、全局加载状态和网络请求，保证系统交互一致性。

#### 3.9.2 功能要求

1. 使用 Pinia 或 Vuex 管理全局状态。
2. 管理当前登录用户信息。
3. 管理当前选中的模型版本和检测参数。
4. 管理全局 Loading 状态。
5. 使用 Axios 封装统一 HTTP 请求实例。
6. 请求拦截器应自动携带 Token。
7. 响应拦截器应统一处理业务错误和系统错误。
8. 前端应通过 Element Plus Message 组件展示统一提示。

## 四、接口设计

### 4.1 接口通用规范

#### 4.1.1 基础路径

系统后端接口统一使用以下前缀：

```text
/api/v1
```

#### 4.1.2 通用响应结构

所有接口应返回统一 JSON 结构：

```json
{
  "code": 200,
  "msg": "success",
  "data": {}
}
```

#### 4.1.3 常用状态码

| code | 说明 |
| --- | --- |
| 200 | 请求成功 |
| 400 | 请求参数错误 |
| 401 | 未登录或 Token 失效 |
| 403 | 无访问权限 |
| 404 | 资源不存在 |
| 413 | 上传文件过大 |
| 500 | 服务内部错误 |
| 503 | 推理服务繁忙或显存资源不足 |

### 4.2 用户登录接口

#### 4.2.1 接口信息

```text
POST /api/v1/auth/login
```

#### 4.2.2 请求参数

```json
{
  "username": "admin",
  "password": "123456"
}
```

#### 4.2.3 响应数据

```json
{
  "code": 200,
  "msg": "success",
  "data": {
    "token": "jwt-token-string",
    "user": {
      "id": 1,
      "username": "admin",
      "role": "admin"
    }
  }
}
```

### 4.3 图片检测接口

#### 4.3.1 接口信息

```text
POST /api/v1/detect/image
```

#### 4.3.2 请求方式

请求类型为 `multipart/form-data`。

| 参数 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| file | File | 是 | 待检测图片 |
| modelVersion | string | 是 | 模型版本 |
| confidence | number | 是 | 置信度阈值 |
| iou | number | 是 | IoU 阈值 |

#### 4.3.3 响应数据

```json
{
  "code": 200,
  "msg": "success",
  "data": {
    "recordId": 1001,
    "imageWidth": 1920,
    "imageHeight": 1080,
    "elapsedMs": 86,
    "objects": [
      {
        "className": "vehicle",
        "confidence": 0.91,
        "bbox": {
          "x": 120,
          "y": 260,
          "width": 80,
          "height": 45
        }
      }
    ],
    "analysis": "画面中检测到车辆目标，目标分布集中在道路区域。"
  }
}
```

### 4.4 视频检测接口

#### 4.4.1 接口信息

```text
POST /api/v1/detect/video
```

#### 4.4.2 请求方式

请求类型为 `multipart/form-data`。

| 参数 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| file | File | 是 | 待检测视频 |
| modelVersion | string | 是 | 模型版本 |
| confidence | number | 是 | 置信度阈值 |
| iou | number | 是 | IoU 阈值 |
| frameInterval | number | 否 | 抽帧间隔 |

#### 4.4.3 响应数据

```json
{
  "code": 200,
  "msg": "success",
  "data": {
    "recordId": 1002,
    "elapsedMs": 1560,
    "frameCount": 120,
    "processedFrames": 24,
    "summary": {
      "totalObjects": 48,
      "classes": {
        "vehicle": 42,
        "person": 6
      }
    }
  }
}
```

### 4.5 检测记录查询接口

#### 4.5.1 接口信息

```text
GET /api/v1/records
```

#### 4.5.2 查询参数

| 参数 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| page | number | 是 | 当前页码 |
| pageSize | number | 是 | 每页数量 |
| startTime | string | 否 | 开始时间 |
| endTime | string | 否 | 结束时间 |
| className | string | 否 | 目标类别 |
| modelVersion | string | 否 | 模型版本 |
| status | string | 否 | 检测状态 |

#### 4.5.3 响应数据

```json
{
  "code": 200,
  "msg": "success",
  "data": {
    "total": 120,
    "page": 1,
    "pageSize": 10,
    "items": [
      {
        "id": 1001,
        "fileName": "demo.jpg",
        "fileType": "image",
        "modelVersion": "yolov11n-onnx",
        "objectCount": 5,
        "elapsedMs": 86,
        "status": "success",
        "createdAt": "2026-06-03 10:30:00"
      }
    ]
  }
}
```

### 4.6 检测详情接口

#### 4.6.1 接口信息

```text
GET /api/v1/records/{id}
```

#### 4.6.2 响应数据

```json
{
  "code": 200,
  "msg": "success",
  "data": {
    "id": 1001,
    "fileName": "demo.jpg",
    "fileHash": "sha256-hash",
    "modelVersion": "yolov11n-onnx",
    "params": {
      "confidence": 0.25,
      "iou": 0.45
    },
    "resultJson": [],
    "analysis": "检测结果分析文本",
    "elapsedMs": 86,
    "status": "success",
    "createdAt": "2026-06-03 10:30:00"
  }
}
```

### 4.7 统计分析接口

#### 4.7.1 接口信息

```text
GET /api/v1/statistics/summary
```

#### 4.7.2 查询参数

| 参数 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| startTime | string | 否 | 开始时间 |
| endTime | string | 否 | 结束时间 |

#### 4.7.3 响应数据

```json
{
  "code": 200,
  "msg": "success",
  "data": {
    "totalRecords": 120,
    "successRecords": 116,
    "failedRecords": 4,
    "classDistribution": [
      {
        "className": "vehicle",
        "count": 320
      }
    ],
    "elapsedTrend": [
      {
        "date": "2026-06-03",
        "avgElapsedMs": 92
      }
    ],
    "modelUsage": [
      {
        "modelVersion": "yolov11n-onnx",
        "count": 80
      }
    ]
  }
}
```

## 五、数据库设计

### 5.1 数据库选型

系统使用 MySQL 作为关系型数据库，后端通过 SQLAlchemy 进行 ORM 映射和数据库操作。数据库连接应使用连接池管理，避免高并发下频繁创建连接。

### 5.2 用户表：sys_user

| 字段名 | 类型 | 约束 | 说明 |
| --- | --- | --- | --- |
| id | bigint | 主键，自增 | 用户 ID |
| username | varchar(64) | 唯一，非空 | 登录账号 |
| password_hash | varchar(255) | 非空 | bcrypt 密码摘要 |
| nickname | varchar(64) | 可空 | 用户昵称 |
| role | varchar(32) | 非空 | 用户角色：user/admin |
| status | tinyint | 默认 1 | 用户状态：1 启用，0 禁用 |
| created_at | datetime | 非空 | 创建时间 |
| updated_at | datetime | 非空 | 更新时间 |

### 5.3 检测日志表：detect_log

| 字段名 | 类型 | 约束 | 说明 |
| --- | --- | --- | --- |
| id | bigint | 主键，自增 | 检测记录 ID |
| user_id | bigint | 外键 | 发起检测的用户 ID |
| file_name | varchar(255) | 非空 | 原始文件名 |
| file_type | varchar(32) | 非空 | 文件类型：image/video/dataset |
| file_hash | varchar(128) | 非空 | 文件哈希 |
| model_version | varchar(64) | 非空 | 模型版本 |
| confidence | decimal(4,3) | 非空 | 置信度阈值 |
| iou | decimal(4,3) | 非空 | IoU 阈值 |
| result_json | json | 可空 | 检测结果 JSON 快照 |
| analysis_text | text | 可空 | AI 研判文本 |
| object_count | int | 默认 0 | 检测目标数量 |
| elapsed_ms | int | 非空 | 检测耗时，单位毫秒 |
| status | varchar(32) | 非空 | 状态：success/failed |
| error_msg | varchar(512) | 可空 | 错误信息 |
| created_at | datetime | 非空 | 创建时间 |

### 5.4 模型配置表：model_config

| 字段名 | 类型 | 约束 | 说明 |
| --- | --- | --- | --- |
| id | bigint | 主键，自增 | 模型配置 ID |
| model_name | varchar(128) | 非空 | 模型名称 |
| model_version | varchar(64) | 唯一，非空 | 模型版本 |
| model_path | varchar(255) | 非空 | 模型文件路径 |
| backend | varchar(32) | 非空 | 推理后端：onnx/tensorrt |
| description | varchar(512) | 可空 | 模型说明 |
| enabled | tinyint | 默认 1 | 是否启用 |
| created_at | datetime | 非空 | 创建时间 |
| updated_at | datetime | 非空 | 更新时间 |

### 5.5 数据库存储要求

1. 用户密码必须以 bcrypt 摘要形式存储，禁止明文保存。
2. 检测结果应保存为 JSON 快照，保证历史记录可回溯。
3. 文件哈希用于识别重复文件和关联检测记录。
4. 常用查询字段应建立索引，例如 `user_id`、`created_at`、`model_version`、`status`。
5. 数据库操作必须通过 ORM 或参数化 SQL 完成，防止 SQL 注入。

## 六、非功能需求

### 6.1 性能需求

1. 图片检测接口在模型已预热情况下，应尽量控制在秒级响应。
2. 检测记录列表应使用服务端分页，单页查询响应时间应满足日常交互需求。
3. 前端渲染检测框时应使用 Canvas，保证多目标场景下仍能保持流畅交互。
4. 统计接口应由后端聚合计算，避免前端加载大量历史数据。

### 6.2 并发需求

1. YOLO 推理属于显存敏感任务，后端应引入线程锁或任务队列机制。
2. 同一时刻进入 GPU 显存计算的任务数量应受控。
3. 当请求量超过系统处理能力时，应排队处理或快速返回 503。
4. 并发控制不应影响登录、记录查询和统计接口的正常访问。

### 6.3 安全需求

1. 所有核心业务接口必须校验 Token。
2. 用户密码必须使用 bcrypt 摘要加密。
3. 上传文件必须校验类型、大小和文件名安全性。
4. 后端应使用 `werkzeug` 对上传文件名进行安全过滤。
5. 数据库访问应使用 SQLAlchemy ORM 或参数化查询。
6. 前端不得在页面中直接渲染未经处理的 HTML 文本，避免 XSS 风险。
7. 系统应限制普通用户访问其他用户的检测记录。

### 6.4 异常处理需求

1. Flask 应注册全局 Error Handler。
2. 图像解码失败时应返回规范错误响应，不得导致后端进程退出。
3. 模型推理 OOM 时应捕获异常并释放临时变量。
4. 大模型调用失败时应记录日志，核心检测结果仍应正常返回。
5. 数据库连接异常时应返回统一错误结构。
6. 前端应对 401、403、500、503 等状态进行统一提示。

### 6.5 可维护性需求

1. 前端应按页面、组件、状态、接口模块进行目录划分。
2. 后端应按路由层、服务层、模型推理层、数据访问层进行模块划分。
3. 检测模型加载、推理和后处理逻辑应封装为独立类或模块。
4. 接口响应格式、错误码和日志格式应保持统一。
5. 配置项应通过配置文件或环境变量管理。

### 6.6 兼容性需求

1. 前端应支持主流 Chromium 内核浏览器。
2. 后端应支持 Python 3.10 及以上版本。
3. 数据库应支持 MySQL 8.0 及以上版本。
4. GPU 推理环境应支持 ONNX Runtime GPU 或 TensorRT。
5. 无 GPU 环境下可提供 CPU 推理模式用于演示或测试，但性能不作为主要验收指标。

## 七、测试与验收标准

### 7.1 功能测试

| 测试项 | 验收标准 |
| --- | --- |
| 用户登录 | 正确账号密码可登录，错误账号密码提示失败 |
| 路由守卫 | 未登录用户无法访问核心业务页面 |
| 图片上传 | 支持 jpg、jpeg、png 文件上传并完成检测 |
| 视频上传 | 支持 mp4 文件上传并返回检测汇总 |
| 参数配置 | 模型版本、置信度、IoU 参数可配置并传至后端 |
| 检测结果展示 | 检测框、类别和置信度可正确绘制在图像上 |
| AI 研判 | 可展示检测结果分析文本，大模型失败不影响检测展示 |
| 记录查询 | 支持时间、类别、模型、状态等条件筛选 |
| 分页查询 | 检测记录使用后端真实分页 |
| 统计分析 | 可展示任务总数、类别分布、耗时趋势等图表 |

### 7.2 接口测试

| 测试项 | 验收标准 |
| --- | --- |
| 响应格式 | 所有接口均返回统一 JSON 结构 |
| 参数校验 | 非法参数返回 400，不进入核心业务流程 |
| 鉴权校验 | 未携带 Token 的核心接口返回 401 |
| 权限控制 | 普通用户无法访问其他用户记录 |
| 文件检测接口 | 合法文件可返回检测结果和耗时信息 |
| 记录接口 | 支持多条件查询和分页返回 |
| 统计接口 | 返回可直接用于 ECharts 渲染的数据结构 |

### 7.3 性能测试

| 测试项 | 验收标准 |
| --- | --- |
| 模型预热 | 后端启动后完成模型加载，检测请求中不重复加载模型 |
| 图片检测耗时 | 常规图片检测可在可接受时间内返回结果 |
| 并发控制 | 多请求同时到达时，系统不会因显存争用崩溃 |
| 记录分页 | 大量检测记录下仍可分页查询 |
| Canvas 渲染 | 多目标检测框展示流畅，无明显页面卡顿 |

### 7.4 安全测试

| 测试项 | 验收标准 |
| --- | --- |
| 密码存储 | 数据库中不出现明文密码 |
| SQL 注入 | 登录、查询等接口无法通过注入方式绕过校验 |
| 文件上传 | 非法扩展名、超大文件、危险文件名被拒绝或安全处理 |
| Token 失效 | Token 失效后用户被提示重新登录 |
| 越权访问 | 普通用户无法查看或操作其他用户数据 |

### 7.5 验收标准

项目最终验收应满足以下条件：

1. 前端页面可正常启动并完成主要业务流程。
2. 后端服务可正常启动并提供 RESTful API。
3. 数据库表结构完整，核心数据可正常写入和查询。
4. 模型推理模块可完成图片目标检测并返回结构化结果。
5. Canvas 可视化结果与原图目标位置保持一致。
6. 检测记录、筛选、分页和详情查看功能可正常使用。
7. 统计图表能够基于真实后端数据展示。
8. 异常场景下系统能够返回规范错误信息，不出现后端进程崩溃。
9. 测试用例覆盖核心功能、接口、安全和性能要求。
10. 项目源码、部署说明、数据库脚本和测试报告完整交付。

## 八、部署与运行要求

### 8.1 前端运行环境

| 项目 | 要求 |
| --- | --- |
| 框架 | Vue 3 + Vite |
| UI 组件库 | Element Plus |
| 状态管理 | Pinia 或 Vuex |
| 网络请求 | Axios |
| 图表库 | ECharts |
| 绘图能力 | HTML5 Canvas API |
| 浏览器 | Chrome、Edge 等主流浏览器 |

### 8.2 后端运行环境

| 项目 | 要求 |
| --- | --- |
| 语言 | Python 3.10+ |
| Web 框架 | Flask |
| 图像处理 | OpenCV |
| 推理框架 | ONNX Runtime GPU 或 TensorRT |
| 数据库访问 | SQLAlchemy、PyMySQL |
| 密码加密 | bcrypt |
| 文件处理 | werkzeug |

### 8.3 数据库环境

| 项目 | 要求 |
| --- | --- |
| 数据库 | MySQL 8.0+ |
| 字符集 | utf8mb4 |
| 连接方式 | SQLAlchemy 连接池 |
| 初始化内容 | 用户表、检测日志表、模型配置表 |

### 8.4 模型与硬件要求

1. 系统应提供 YOLO 系列模型权重文件，例如 YOLOv11n ONNX 模型。
2. GPU 推理环境应安装匹配版本的 CUDA、cuDNN 和 ONNX Runtime GPU。
3. TensorRT 部署场景应提供对应 engine 文件或转换脚本。
4. CPU 环境可用于演示和功能测试，但检测耗时可能明显增加。
5. 模型文件路径、推理后端和默认阈值应支持配置化管理。

### 8.5 启动要求

1. 前端通过 Vite 启动开发服务或构建静态资源。
2. 后端启动时应初始化数据库连接和模型实例。
3. 后端启动失败时应输出明确日志，包括模型文件不存在、数据库连接失败等原因。
4. 前端 API 基础地址应可通过环境变量配置。
5. 生产部署时应避免在前端暴露敏感配置。

## 九、项目交付物

项目交付内容包括：

1. 前端项目源码。
2. 后端项目源码。
3. 数据库建表脚本和初始化数据脚本。
4. YOLO/ONNX 模型文件或模型下载说明。
5. RESTful API 接口文档。
6. 项目部署运行说明。
7. 测试用例和测试报告。
8. 用户操作说明。
9. 项目说明书和需求规格说明书。

## 十、风险与约束

### 10.1 技术风险

1. GPU 显存不足可能导致推理失败或服务不稳定。
2. 大尺寸航拍图像可能导致图像解码和 Canvas 渲染压力较大。
3. 视频检测任务耗时较长，需要合理控制抽帧策略。
4. 外部 LLM API 的稳定性和响应时间不可完全控制。
5. 模型检测精度受训练数据、场景差异和目标尺寸影响。

### 10.2 应对措施

1. 通过模型预热、线程锁或任务队列控制 GPU 推理并发。
2. 对上传文件大小和格式进行限制。
3. 视频检测提供抽帧间隔配置，避免全量逐帧推理导致阻塞。
4. 大模型调用采用异步或后台线程，并设置超时与异常兜底。
5. 检测结果保存 JSON 快照，便于后续复核和模型优化。

## 十一、版本记录

| 版本 | 日期 | 说明 |
| --- | --- | --- |
| V1.0 | 2026-06-03 | 初始项目说明书，覆盖功能模块、接口、数据库、测试与验收标准 |